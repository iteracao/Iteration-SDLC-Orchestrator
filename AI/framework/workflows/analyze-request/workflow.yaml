code: analyze-request
name: Analyze Request
phase: analysis

purpose: >
  Analyze a requirement or request against current solution knowledge,
  real repository evidence, and SDLC framework guidance, then produce
  a plain Markdown analysis report.

primaryAgent: solution-analyst

requiredInputs:
  - name: requestTitle
    type: string
    required: true
  - name: requestDescription
    type: string
    required: true
  - name: targetSolutionId
    type: guid
    required: true

knowledgeReads:
  - context/overview.md
  - business/business-rules.md
  - business/workflows.md
  - architecture/architecture-overview.md
  - architecture/module-map.md
  - history/decisions.md
  - history/open-questions.md
  - history/known-gaps.md

producedArtifacts:
  - type: analysis-report
    name: Analysis Report

knowledgeUpdates:
  - analysis/latest-analysis.md

executionRules:
  - Use prompt-driven analysis instead of JSON workflow input or output contracts.
  - Use solution knowledge as current truth unless repository evidence clearly contradicts it.
  - Use repository evidence as proof layer, not as replacement for canonical docs.
  - Log every prompt and response for the three analysis phases.
  - Persist only the final Markdown report artifact as analysis-report.md.
  - Keep analysis concise, evidence-based, and structured.

nextWorkflows:
  - design-solution-change
