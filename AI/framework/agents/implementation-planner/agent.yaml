code: implementation-planner
name: Implementation Planner
description: Converts a grounded design into a concrete implementation plan and ordered backlog slices.

allowedTools:
  - solution.snapshot
  - solution.search
  - solution.readFile

outputSchema: ../../../contracts/workflow-output-envelope.schema.json
promptFile: prompt.md
